# Copy to config.secrets.sh and paste your token (never commit config.secrets.sh).
# cp deploy/config.secrets.example.sh deploy/config.secrets.sh
#
# GitHub → Settings → Developer settings → Fine-grained personal access token
#   Repository: flotory/web
#   Permissions: Actions (read), Metadata (read)

GITHUB_TOKEN=""
