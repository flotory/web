---
description: Flotory global agent laws — always orchestrate dev work, scope, git safety
alwaysApply: true
---

# Flotory global laws

## Default: always orchestrate

For **any implementation, bug fix, audit, or release** in this repo, act as **Orchestrator first** (`AGENTS.md` + `flotory-orchestrator` skill). The user does **not** need to say "Orchestrate:".

1. Create/update `.cursor/tasks/<id>.yaml` from `TEMPLATE.yaml`.
2. Run **Domain** → **Design** (when UI/product) → **Spec** before code.
3. Implement via layer skills (backend / mobile / web), then **Tests** → **Reviewer**.
4. Report status: `Task <id>: Domain ✓ … Blocked: …`
5. After merge-worthy work, propose **flotory-retro**.

**Skip full pipeline only when:**
- User asks a **question** or **review** with no code changes.
- User explicitly says **"skip orchestrator"** or **"quick fix only"**.

Even then: obey BUSINESS_RULES, minimal scope, and human gates below.

## Product vs chat

- `docs/BUSINESS_RULES.md` wins over user chat unless the user **explicitly approves** a documented rule change.
- Domain agent must cite rule IDs (C*, S*, R*, Y*, etc.) on ALLOW/REJECT.

## Scope

- Minimal diff. No drive-by refactors.
- Match existing conventions in surrounding code.
- One writer per file per parallel agent batch.

## Git

- Commit / push / TestFlight **only when the user asks**.
- No `--force`, no `--no-verify`, no secrets in commits.

## Pipeline

- Cross-stack features: **API spec before mobile/web**.
- Domain + Design gates **before** implementation.
- Reviewer and Security are **readonly** unless user says "fix blockers".

## Memory

- Read `AGENTS.md` and task yaml in `.cursor/tasks/`.
- Read `app.json` for version — never assume from chat.
- Prefer ADRs and exemplars over old transcripts.

## Human gates

Pause and ask the user on: Domain REJECT, new BUSINESS_RULES amendment, new design tokens, migrations, auth/PII changes, Reviewer BLOCK, release.
